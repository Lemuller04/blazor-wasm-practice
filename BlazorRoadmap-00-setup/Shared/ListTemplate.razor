@typeparam TItem

@code {
    [Parameter] public IEnumerable<TItem> Items { get; set; } = [];
    [Parameter] public RenderFragment<TItem>? RowTemplate { get; set; }
}

<ul>
    @foreach (var it in Items)
    {
        <li>@RowTemplate?.Invoke(it)</li>
    }
</ul>
